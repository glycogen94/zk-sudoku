{
  "name": "@zk-sudoku/core",
  "version": "0.1.0",
  "description": "ZK-SNARK for Sudoku core functionality",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "./build.sh",
    "build:rust": "cd rust && wasm-pack build --target bundler --release",
    "build:ts": "tsc",
    "dev": "tsc -w",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^20.5.9",
    "eslint": "^8.48.0",
    "typescript": "^5.2.2",
    "wasm-pack": "^0.12.1"
  }
}
